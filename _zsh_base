# Require any private files
for dotfile in ~/dotfiles/private/_zsh_*; do
  source ${dotfile}
done

for dotfile in ~/dotfiles/private/_universal_*; do
  source ${dotfile}
done

# Require any universal dotfiles
for dotfile in ~/dotfiles/_universal_*; do
  source ${dotfile}
done
